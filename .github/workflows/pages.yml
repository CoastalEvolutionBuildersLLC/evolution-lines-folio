# This workflow is disabled - deployment is handled by Netlify
# Netlify auto-deploys from the main branch via netlify.toml
# No GitHub Pages deployment is needed.

name: Disabled - Netlify handles deployment

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deployment is handled by Netlify. See netlify.toml for config."
