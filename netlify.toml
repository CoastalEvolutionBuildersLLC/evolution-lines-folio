[build]
  command = "npm install && npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "dist/server"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/main"
  status = 200
