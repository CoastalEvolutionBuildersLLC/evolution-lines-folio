[build]
  command = "npm install && npm run build && node scripts/generate-index.js"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  NITRO_PRESET = "netlify"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
